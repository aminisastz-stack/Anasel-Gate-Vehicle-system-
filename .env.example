# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="AIzaSyAM32b5rNCqGfaAlqFgy7cFIENJwnfw01g"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Database Configuration
DB_USER= "anasel"
DB_PASSWORD="AnaselSecurity2025"
DB_HOST= "45.88.188.129"
DB_PORT= "5332"
DB_NAME= "postgres"

# GitHub Personal Access Token (PAT) for authenticated pushes via HTTPS
# NEVER commit your actual token to version control.
# Usage: export GITHUB_PAT before running push scripts.
GITHUB_PAT=""

# Internal (Colify) Database Connection
# Prefer a single DATABASE_URL for internal connectivity.
# Example format:
#   postgres://USER:PASSWORD@HOST:5432/DB_NAME
# Set this in your Colify environment (do not commit real values).
DATABASE_URL=""
# Optional SSL toggle (some hosted environments require SSL)
# Set to 'true' to enable SSL with rejectUnauthorized=false
DB_SSL="false"
